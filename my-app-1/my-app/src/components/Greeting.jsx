const Greeting = (props) => {


    return(
        <>
            <h1>the name { props.intro } contains {props.intro.length} characters</h1>
            
     
        </>
    );
};

export default Greeting;